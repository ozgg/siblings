<section class="diary">
  <hgroup>
    <h2><%= diary_entry_link(date, l(date)) %></h2>
    <p><%= l(date, format: '%A') %></p>
  </hgroup>
  <% posts.each do |entity| %>
    <%= render partial: 'diary/entry_preview', locals: { entity: } %>
  <% end %>
</section>
